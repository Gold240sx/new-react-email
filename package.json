{
	"name": "emails",
	"version": "0.0.18",
	"private": true,
	"workspaces": [
		".react-email"
	],
	"scripts": {
		"dev": "email dev",
		"export": "email export",
		"install:all": "npm install && cd react-email-starter && npm install",
		"dev:root": "next dev",
		"dev:preview": "cd react-email-starter && npm run dev",
		"format:check": "prettier --check \"**/*.{ts,tsx,md}\"",
		"format": "prettier --write \"**/*.{ts,tsx,md}\"",
		"build": "next build",
		"start": "next start"
	},
	"dependencies": {
		"@react-email/components": "0.0.6",
		"react-email": "1.9.3"
	}
}
